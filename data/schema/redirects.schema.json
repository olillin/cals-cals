{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "redirects": {
            "type": "object",
            "description": "A mapping of redirects, where each key is the path to redirect from and the value the destination path",
            "additionalProperties": {
                "type": "string"
            }
        }
    },
    "required": ["redirects"]
}
